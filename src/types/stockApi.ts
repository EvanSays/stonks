export type Data = {
    description: string;
    displaySymbol: string;
    symbol: string;
}

export type TickerPrice = {
    currPrice: string;
    change: string;
    percentChange: string;
    dayHigh: string;
    dayLow: string;
    dayOpen: string;
    prevClose: string;
}